int x;
void() main() {
    int n := 3;
    int m := 2;
    prints(mulIzq(n,m));
};

int(int, int) mulDer(a,b) {
    if ( b == 0) {
        return 0;
    } elsif (b == 1) {
       return a;
    } else {
         return a + mulIzq(a,b-1);
    }
};


int(int, int) mulIzq(a,b) {
    int($ int, $ int) sum(a,b) {
       int c := 0;
       while( a > 0) {
            a := a - 1;
            c := c + 1;
       }

       for( int i := 0; i < b; i++) {
            c := c + 1;
       }
    };

    if ( a == 0) {
        return 0;
    } elsif (a == 1) {
        return b;
    } else {
        return sum(b ,mulDer(a-1,b));
    }
};

