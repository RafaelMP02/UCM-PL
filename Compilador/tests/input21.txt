int x;
int y;
void() main(){
    x := 0;
    y := x + 1;
    prints(y);
    x := f(y); //es la global y por lo que aunque seria ocultada no lo sera
    prints(y);
    prints(x);
};
int z;
int w;
int(int) f(y){ //oculta a la global y
    z := y;
    prints(z);
    y := y + 1;
    prints(y);
    return 2*z;
};