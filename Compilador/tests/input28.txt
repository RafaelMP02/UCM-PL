class A{
    int x;

    int() getX(){
        return x;
    };

    void(int) setX(a){
        x := a;
    };

    void() incrementX(){
            x := x + 1;
    };
};

void() main() {
    class A s;
    s.x := 1;
    prints(s.x);
    prints(s.getX());
    s.incrementX();
    prints(s.x);
};